<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".CloudConfVarnaHomePageActivity" >
    
    <!-- 
    <TextView
        android:id="@+id/appTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world" />
    
    <RelativeLayout 
        android:id="@+id/filterWrapper"
        android:layout_below="@id/appTitle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content">
        
        <EditText 
            android:id="@+id/filterTextBox"
            android:hint="@string/filter_beer_hint"
            style="@style/StandartEditTextBox">
        </EditText>
        
        <Button 
            android:layout_below="@id/filterTextBox"
            android:id="@+id/filterButton"
            android:text="@string/filter"
            style="@style/StandartHorizontalButton"/>
        
    </RelativeLayout>
    -->
    
	<ListView
		android:id="@android:id/list"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"/>

</RelativeLayout>
